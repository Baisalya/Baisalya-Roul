{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/validate-release.mjs",
    "test:dist": "node scripts/validate-dist.mjs",
    "check": "npm run test && npm run build && npm run test:dist"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
